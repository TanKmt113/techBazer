/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com','image.uniqlo.com','png.pngtree.com','cdn.dummyjson.com','localhost','im.uniqlo.com'],
      },
    reactStrictMode: false
};

export default nextConfig;
